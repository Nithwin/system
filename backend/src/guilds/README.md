# Guilds Module (Backend)

## Purpose
This module will handle the "Party/Guild" system logic.

## To-Do
- [ ] Create `Guild` entity (Name, Level, Members).
- [ ] Create `GuildsService` for CRUD operations.
- [ ] Create `GuildsController` with endpoints:
    - `POST /guilds` (Create Guild)
    - `POST /guilds/join` (Join Guild)
