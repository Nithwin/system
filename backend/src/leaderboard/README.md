# Leaderboard Module (Backend)

## Purpose
This module will handle global and friend leaderboards.

## To-Do
- [ ] Create `LeaderboardService` to aggregate user stats.
- [ ] Create `LeaderboardController` with endpoints:
    - `GET /leaderboard/global`
    - `GET /leaderboard/friends`
